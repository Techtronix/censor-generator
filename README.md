# InspIRCd censor.conf Generator
This Python script does exactly what its name infers - it generates a censor.conf file for InspIRCd.

To see the available help options, run the script with `--help`.
